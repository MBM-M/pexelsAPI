<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pexels Featured Collections</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    h1 { text-align: center; }
    .collection { margin: 20px 0; border: 1px solid #ddd; padding: 10px; border-radius: 5px; }
    .collection img { max-width: 100%; height: auto; border-radius: 5px; }
    .collection-name { font-weight: bold; font-size: 1.2em; margin: 10px 0; }
    .collection-description { font-style: italic; }
  </style>
</head>
<body>

  <h1>Pexels Featured Collections</h1>

  <div class="collections">
    <% @collections.each do |collection| %>
      <div class="collection">
        <!-- Accessing cover photo from JSON structure -->
        <img src="<%= collection['cover_photo']['src']['original'] %>" alt="<%= collection['title'] %>">
      </div>
    <% end %>
  </div>

</body>
</html>

